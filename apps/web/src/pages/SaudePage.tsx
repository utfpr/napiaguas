import { SaudeRealDataPage } from './SaudeRealDataPage'

/**
 * Página principal do GT Saúde
 * Agora conectada com dados reais da API
 */
export const SaudePage = () => {
  return <SaudeRealDataPage />
}
